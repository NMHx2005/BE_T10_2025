extends ../../layouts/auth

block content
  //- Thẻ ngoài: logo + form (Tailwind có dấu ":" / "/" — phải dùng class="..." để Pug không parse nhầm)
  .auth-card(class="p-8 sm:p-10")
    //- Header
    a.inline-flex.items-center.gap-2.transition-colors.text-sm.font-medium.mb-8(href="/", class="text-[#323842] hover:text-[#f48525]")
      i.fa-solid.fa-arrow-left
      span Về trang chủ

    .text-center.mb-8
      .inline-flex.items-center.justify-center.w-14.h-14.text-white.text-2xl.shadow-lg.mb-4(class="bg-[#f48525]")
        i.fa-solid.fa-right-to-bracket
      h1(class="text-2xl sm:text-3xl font-bold text-[#0d0d0d] tracking-tight") Đăng nhập
      p(class="text-[#323842] mt-2 text-sm sm:text-base") Chào mừng trở lại. Đăng nhập để tiếp tục mua sắm.

    //- Thông báo lỗi / thành công (do JS điền)
    #auth-alert.hidden.mb-6.rounded-xl.px-4.py-3.text-sm.font-medium(role="alert")

    form#login-form.space-y-5
      div(class="space-y-1.5")
        label.block.text-sm.font-semibold(for="email" class="text-[#181b20]") Email
        .relative
          i.fa-regular.fa-envelope.absolute.left-3.text-sm.pointer-events-none(class="top-1/2 -translate-y-1/2 text-[#323842]")
          input#email.auth-input(type="email", name="email", placeholder="you@example.com", required, autocomplete="email")

      div(class="space-y-1.5")
        .flex.items-center.justify-between.gap-2
          label.block.text-sm.font-semibold(for="password" class="text-[#181b20]") Mật khẩu
          a.text-sm.font-medium(href="/auth/forgot-password", class="text-[#f48525] hover:text-[#e07820]") Quên mật khẩu?
        .relative
          i.fa-solid.fa-lock.absolute.left-3.text-sm.pointer-events-none(class="top-1/2 -translate-y-1/2 text-[#323842]")
          input#password.auth-input.pr-12(type="password", name="password", placeholder="••••••••", required, autocomplete="current-password")
          button#toggle-password.absolute.right-2.p-2(type="button", aria-label="Hiện mật khẩu", class="top-1/2 -translate-y-1/2 text-[#323842] hover:text-[#0d0d0d]")
            i.fa-regular.fa-eye

      label.flex.items-center.gap-2.cursor-pointer.select-none
        input.rounded(type="checkbox", name="remember" class="border-[#dee1e6] text-[#f48525]")
        span.text-sm(class="text-[#323842]") Ghi nhớ đăng nhập

      button#login-submit.auth-btn-primary(type="submit")
        span#login-submit-text Đăng nhập
        span#login-submit-loading.hidden.items-center.gap-2
          i.fa-solid.fa-circle-notch.fa-spin
          span Đang xử lý...

    .relative.my-8
      .absolute.inset-0.flex.items-center
        .w-full.border-t(class="border-[#eef0f3]")
      .relative.flex.justify-center
        span(class="bg-white/90 px-3 text-xs font-medium text-[#8c8f95] uppercase tracking-wider") hoặc

    p.text-center.text-sm(class="text-[#323842]")
      | Chưa có tài khoản?
      |
      a.font-semibold.ml-1(href="/auth/register", class="text-[#f48525] hover:text-[#e07820]") Đăng ký ngay

block scripts
  script.
    (function () {
      var API_LOGIN = '/api/v1/auth/login';
      var form = document.getElementById('login-form');
      var submitBtn = document.getElementById('login-submit');
      var submitText = document.getElementById('login-submit-text');
      var submitLoading = document.getElementById('login-submit-loading');
      var pwdInput = document.getElementById('password');
      var togglePwd = document.getElementById('toggle-password');

      function showAlert(type, message) {
        if (!window.AppNotify) return;
        if (type === 'error') return window.AppNotify.error(message);
        return window.AppNotify.success(message);
      }

      function hideAlert() {
        return;
      }

      function formatApiError(data) {
        if (!data) return 'Đăng nhập thất bại. Vui lòng thử lại.';
        if (data.errors && Array.isArray(data.errors) && data.errors.length) {
          return data.errors.map(function (e) { return e.message || e.msg || ''; }).filter(Boolean).join(' ') || data.message;
        }
        return data.message || 'Đăng nhập thất bại.';
      }

      function setLoading(loading) {
        submitBtn.disabled = loading;
        if (loading) {
          submitText.classList.add('hidden');
          submitLoading.classList.remove('hidden');
          submitLoading.classList.add('inline-flex');
        } else {
          submitText.classList.remove('hidden');
          submitLoading.classList.add('hidden');
          submitLoading.classList.remove('inline-flex');
        }
      }

      togglePwd.addEventListener('click', function () {
        var isPwd = pwdInput.type === 'password';
        pwdInput.type = isPwd ? 'text' : 'password';
        togglePwd.querySelector('i').className = isPwd ? 'fa-regular fa-eye-slash' : 'fa-regular fa-eye';
      });

      form.addEventListener('submit', async function (e) {
        e.preventDefault();
        hideAlert();

        var email = document.getElementById('email').value.trim();
        var password = document.getElementById('password').value;

        if (!email || !password) {
          showAlert('error', 'Vui lòng nhập email và mật khẩu.');
          return;
        }

        setLoading(true);
        try {
          var res = await fetch(API_LOGIN, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email: email, password: password }),
          });
          var result = await res.json().catch(function () { return {}; });

          if (!res.ok) {
            throw new Error(formatApiError(result));
          }

          if (result.tokens && result.tokens.accessToken) {
            localStorage.setItem('accessToken', result.tokens.accessToken);
            localStorage.setItem('refreshToken', result.tokens.refreshToken || '');
            localStorage.setItem('user', JSON.stringify(result.user || {}));
          }

          showAlert('success', result.message || 'Đăng nhập thành công. Đang chuyển trang...');
          setTimeout(function () {
            window.location.href = '/profile';
          }, 600);
        } catch (err) {
          showAlert('error', err.message || 'Có lỗi xảy ra.');
        } finally {
          setLoading(false);
        }
      });
    })();
